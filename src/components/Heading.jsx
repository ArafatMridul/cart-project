/* eslint-disable react/prop-types */
const Heading = ({ children }) => {
    return (
        <h1 className="font-fw-bold text-rose-900 text-4xl">
            {children}
        </h1>
    );
};

export default Heading;
